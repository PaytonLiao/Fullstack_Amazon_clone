{
  "name": "amazon_clone",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "devDependencies": {
    "cross-env": "^7.0.3"
  },
  "scripts": {
    "build": "npm install cross-env && cd backend && npm install && npm run build  && cd ../frontend && npm install && npm run build",
    "start": " set TG_ALLOW_MIXED=ALLOW && node backend/build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
